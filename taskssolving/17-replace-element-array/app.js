let allTypes = [
    'string',
    'number',
    'boolean',
    'undefined',
    'null',
    'symbol',
    'object',
    'function'
];

console.log(allTypes);

allTypes [2] =100

let length = allTypes.length

console.log(allTypes,length)
